# pnpm 설정
# 엄격한 peer dependencies 검사 비활성화 (Next.js와의 호환성)
strict-peer-dependencies=false

# 자동으로 peer dependencies 설치
auto-install-peers=true

# shamefully-hoist를 false로 설정하여 pnpm의 격리된 node_modules 구조 유지
shamefully-hoist=false

# 빌드 스크립트 자동 승인 (sharp, unrs-resolver 등)
# 보안상 필요한 패키지만 승인
enable-pre-post-scripts=true
